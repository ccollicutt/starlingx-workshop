# Paste the below into https://etherpad.openstack.org/p/stx-workshop-access
{% for host in groups['tag_stx_workshop'] %}
{{ hostvars[host].ansible_bond0.ipv4.address }},{{ student_name }},{{ student_password }},http://{{ hostvars[host].ansible_bond0.ipv4.address }}:8080
{% endfor %}